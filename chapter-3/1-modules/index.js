/** IMPORTAÇÃO DE MÓDULOS
 *
 * const { printName, lastName } = require('./printName');
 * printName(`Maby ${lastName}`);
 */

//IMPORTAÇÃO DE MÓDULOS NATIVOS
const os = require('os');
console.log(os.type());
